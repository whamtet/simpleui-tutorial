{
	"name": "template-worker-router",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"test": "vitest",
		"styles-watch": "tailwindcss -i src/input.css -o frontend/styles.css --watch",
		"styles": "tailwindcss -i src/input.css -o frontend/styles.css"
	},
	"dependencies": {
		"itty-router": "^4.0.23"
	},
	"devDependencies": {
		"tailwindcss": "^3.3.5",
		"vitest": "^0.31.0",
		"wrangler": "^3.0.0"
	}
}
